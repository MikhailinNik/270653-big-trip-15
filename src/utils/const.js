const DateFormat = {
  MONTH_DAY: 'MMM DD',
  HOURS_MINUTE: 'HH:mm',
  DAY: 'DD',
  HOUR: 'HH',
  MINUTE: 'mm',
};

const KeyboardKey = {
  ESCAPE: 'Escape',
  ESC: 'Esc',
};

const FormEditMode = {
  DEFAULT: 'DEFAULT',
  EDITING: 'EDITING',
};

const SortType = {
  DEFAULT: 'default',
  TIME: 'time',
  PRICE: 'price',
};

export {
  DateFormat,
  KeyboardKey,
  FormEditMode,
  SortType
};
