const DateFormat = {
  MONTH_DAY: 'MMM DD',
  HOURS_MINUTE: 'HH:mm',
  DAY: 'DD',
  HOUR: 'HH',
  MINUTE: 'mm',
};

const KeyboardKey = {
  ESCAPE: 'Escape',
  ESC: 'Esc',
};

const pointMode = {
  DEFAULT: 'DEFAULT',
  EDITING: 'EDITING',
};

export {
  DateFormat,
  KeyboardKey,
  pointMode
};
