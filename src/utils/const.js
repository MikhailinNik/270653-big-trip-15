const DateFormat = {
  MONTH_DAY: 'MMM DD',
  HOURS_MINUTE: 'HH:mm',
  DAY: 'DD',
  HOUR: 'HH',
  MINUTE: 'mm',
};

const KeyboardKey = {
  ESCAPE: 'Escape',
  ESC: 'Esc',
};

const FormEditMode = {
  DEFAULT: 'DEFAULT',
  EDITING: 'EDITING',
};

const SortType = {
  DAY: 'sort-day',
  TIME: 'sort-time',
  PRICE: 'sort-price',
};

const FormMode = {
  EDIT: 'EDIT',
  ADD: 'ADD',
};

export {
  DateFormat,
  KeyboardKey,
  FormEditMode,
  SortType,
  FormMode
};
